# Synth Experiment

Very early prototype of what eventually became [CAW](https://github.com/gridbugs/caw).

Run `cargo run` in the `sdl2` folder to run the synth, and then use the
computer keyboard to play notes and the mouse cursor to control the filter.
